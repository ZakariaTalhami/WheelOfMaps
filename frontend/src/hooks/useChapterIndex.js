import { useEffect } from "react";
import { useSelector } from "react-redux";

const useChapterIndex = (state) => {
    const bookState = useSelector((state) => state.Books);

    return;
};

export default useChapterIndex;

import React from "react";
import classes from "./Timeline.module.scss";
import cs from "classnames";

const Timeline = ({ className }) => {
  return <div className={cs(className)}></div>;
};

export default Timeline;
